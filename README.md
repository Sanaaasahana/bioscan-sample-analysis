# BioScan Sample Analysis

This repository contains a simple sample-analysis script (`analyze.py`) which writes `output.csv`.
Used for Jenkins demo: clone → run → archive `output.csv`.

testing webhook 
